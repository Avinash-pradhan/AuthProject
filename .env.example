# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=authproject.onrender.com,localhost,127.0.0.1

# Database (local development)
# For Render, DATABASE_URL will be set automatically
# DATABASE_URL=postgresql://user:password@localhost:5432/authproject

# Email Settings for Production (Gmail SMTP example)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@yourapp.com

# Render deployment
RENDER_EXTERNAL_HOSTNAME=authproject-xxxxx.onrender.com
